import { Note } from '@/components/mdx'

export const description = 'The Kryptic Ruby package - Kryptic.inject! into ENV.'

# Ruby

The `kryptic-daemon-client` gem injects your project's secrets into `ENV` during
development startup. Outside development it is a no-op, and it never raises.

Source: [github.com/dev-kryptic/Kryptic.Ruby](https://github.com/dev-kryptic/Kryptic.Ruby).
`require "kryptic"` loads the `Kryptic` module.

## Install

```ruby
# Gemfile
gem "kryptic-daemon-client", group: :development
```

## Use

```ruby
# config/application.rb (Rails) or the top of your entry point
require "kryptic"
Kryptic.inject! if Rails.env.development?

ENV["DATABASE_URL"]
```

## Behavior

- No-op when `RAILS_ENV`/`RACK_ENV`/`APP_ENV`/`ENVIRONMENT` is production or staging, or when `KRYPTIC_DISABLED=true`.
- Finds `kryptic.json` by walking up from the working directory.
- Never overwrites ENV entries that are already set.
- Returns a `Result` struct with `injected`, `skipped`, `reason`.
- macOS/Linux via unix socket, Windows via named pipe.

## Options

```ruby
Kryptic.inject!(
  environment: "staging",   # default: kryptic.json defaultEnvironment
  project_id: "proj_x",     # default: kryptic.json projectId
  timeout_ms: 2000,
)
```

<Note>
  Environment variables override everything: `KRYPTIC_PROJECT_ID`, `KRYPTIC_ENV`,
  `KRYPTIC_SOCKET_PATH`, `KRYPTIC_TIMEOUT_MS`, `KRYPTIC_DISABLED`, `KRYPTIC_SILENT`.
</Note>
